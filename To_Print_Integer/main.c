#include <stdio.h>
int main() {
    int number;

   //It is a function used to output/print text to the screen
    printf("Enter an integer: ");

    // reads and stores input
    scanf("%d", &number);

    // displays output
    printf("You entered: %d", number);

    return 0;
}
